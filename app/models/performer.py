"""
models/performer.py — RETIRED (2026-07-09 rename).

The former `Performer`/`PerformerArtist` classes were renamed to
`Artist`/`ArtistCanonical` and now live in artist.py; the former `Artist`
(grouping node) is now `CanonicalArtist` in canonical_artist.py.

This file is no longer imported anywhere. It can't be deleted from the
sandbox — remove it in your terminal:  git rm app/models/performer.py
"""
